# Anthropic 뉴스 — 2026년 9월

> 마지막 업데이트: 2026-09-21  
> 소스: https://platform.claude.com/docs/en/release-notes/overview, https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md

---

## 주요 소식

### 2026-09-18
- **Compliance API — Claude in Chrome 세션 트랜스크립트 추가** — `product_surface: "claude_in_chrome"` 세션 트랜스크립트 반환 지원 (Claude Enterprise 베타). 기존 Compliance Access Key + `read:compliance_user_data` 스코프 사용.

### 2026-09-14
- **Messages API 온디맨드 컴팩션** — `compact-2026-09-04` 베타 헤더로 활성화. `compaction` 파라미터 전송 시 signed compaction block 반환. 이후 요청에서 이전 메시지 대체 가능.

### 2026-09-10
- **Managed Agents `auto` 권한 정책** — 서버 측 각 에이전트·MCP 툴 호출 자동 평가 (실행/거부/승인 대기).
- **ant CLI v1.32.0**: `ant beta:sessions connect` — 터미널에서 Managed Agents 세션 연결 및 실시간 상호작용. `--web` 플래그로 로컬 Console 뷰어 제공.

### 2026-09-03
- **ant CLI v1.30.0**: `ant apply` — 리포지토리 파일로 에이전트·환경·스킬 등 선언적 관리. `claude-lock.json` 잠금 파일로 CI 재현 가능.
- **Per-message effort (베타)** Google Cloud 지원 — Fable 5.1, Mythos 5.1, Opus 5 대상.

### 2026-09-01
- **Claude Fable 5.1** (`claude-fable-5-1`) 공식 출시 — Fable 5 후속. 1M 컨텍스트, $10/$50/MTok, 캐시 읽기 $0.25/MTok. 텍스트 워터마크·C2PA 내장. 30일 데이터 보존 필수.
- **Claude Mythos 5.1** (`claude-mythos-5-1`) 출시 — Project Glasswing 참여자 대상. Fable 5.1과 동일 사양·가격.

---

## Claude Code CLI 주요 릴리즈

| 버전 | 주요 사항 |
|------|---------|
| v2.1.278 | Auto mode 서버 측 분류기 기본 활성화 — 분류기 오버헤드 미과금. `/status`에 `Auto mode server` 행 추가 |
| v2.1.277 | AGENTS.md 지원 — CLAUDE.md 없는 프로젝트에서 대체 읽기; `send-now` 키(Ctrl+Enter) 턴 중단 후 즉시 전송 |
| v2.1.276 | `ANTHROPIC_BASE_URL` 프록시 400 오류 회귀 수정 (v2.1.275 도입) |
| v2.1.275 | 게이트웨이 로그인 계정 확인; claude.ai 스킬·플러그인 터미널 세션 동기화; `send-now` 키(Ctrl+Enter); 다수 버그 수정 |
| v2.1.270 | Bash 읽기 전용 git 명령 권한 요청 회귀 수정 (v2.1.269 도입) |
| v2.1.269 | `claude plugin eval` 추가(플러그인 평가 스위트·채점, JSON+HTML 보고서); `/output-style` 명령; Bash 편집 diff 표시; `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` 설정; [VSCode] 에이전트 맵, Hooks·권한 규칙 대화상자 추가; [웹] 큐 메시지 회수; 다수 버그 수정 |
| v2.1.268 | Claude apps 게이트웨이 `pricing:` 설정; `gatewayInternalNetworks` 관리형 설정; 아티팩트 브라우저 탭 아이콘; `claude plugin` 명령 `--json` 플래그; 서드파티 HTTP 400 회귀 수정; WebFetch 300초 타임아웃 |
| v2.1.263 | 버그 수정 및 안정성 개선 |
| v2.1.261 | `/skill-doctor`(미사용 스킬 분석); `bashOutputMaxChars`·`taskOutputMaxChars` 설정; `--append-subagent-system-prompt-file` 플래그; VS Code diff 패널 외 스크롤, 권한 프롬프트 폴드, 세션 Archive 등 |
| v2.1.260 | 풀스크린 `/diff` 패널; 캐시 미스 원인 표시; `/advisor` 헤드리스·RC 텍스트 폼 지원; 다수 권한·프록시·Fable 캐싱 수정 |
| v2.1.259 | `managedMcpServers` 조직 MCP 배포; `--permission-prompts none`; GitLab MR (`glab mr`) 배지; 동시 세션 `.claude.json` 충돌 수정; Bash 거부 규칙 강화 |
| v2.1.258 | macOS 12 Monterey 실행 불가 회귀 수정 |
| v2.1.257 | **Claude Fable 5.1** 출시; `timeFormat`·`timeZone` 설정; Auto Mode Containment Escape 규칙; `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` |
