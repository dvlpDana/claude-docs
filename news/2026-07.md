# Anthropic 뉴스 — 2026년 7월

> 마지막 업데이트: 2026-07-27  
> 소스: https://www.anthropic.com/news

---

## 주요 소식

### 2026-07-20 이후 (CLI CHANGELOG 기준 확인, 공식 뉴스 페이지 접근 불가)
- **Claude Opus 5** (`claude-opus-5`) 출시 — 새 기본 Opus 모델. 1M 컨텍스트. Fast Mode $10/$50/MTok. Claude Code v2.1.219에 반영됨. 공식 발표: anthropic.com 확인 필요.
- **Opus 4.7 Fast Mode 완전 제거** — v2.1.219(2026-07-24 예고 기준 실행). `speed: "fast"` + `claude-opus-4-7` 요청 시 오류 반환. Opus 5·Opus 4.8 마이그레이션 권고.

### 2026-07-15
- **미드-대화 시스템 메시지 GA** — Claude Fable 5·Mythos 5·Opus 4.8에서 Claude API·Amazon Bedrock·Google Cloud 모두 베타 헤더 없이 사용 가능. 기존 가용성 안내 오류 정정.

### 2026-07-14
- **Claude Enterprise 사용자 관리 Admin API 베타** — 멤버 조회·역할 변경·초대 관리, 그룹·커스텀 역할 관리 지원. 베타 헤더: `anthropic-beta: ce-user-management-2026-07-13`.

### 2026-07-10
- **Access Transparency CMEK 문서 업데이트** — `cmek_preserve` 이벤트에 필터 예시·이벤트 페이로드·보존 사유 코드(`policy_violation_investigation`, `csae_report`) 공식 추가. 자동 안전 파이프라인 포함 이벤트 기록 명시.

### 2026-07-08
- **API 키 만료일 설정 기능 출시** — Console에서 API 키·Admin API 키 생성 시 만료 기간 지정 가능. 7일 이상 키는 만료 전 이메일 알림. Admin API `expires_at` 필드 추가. 기존 키 영향 없음.

### 2026-07-02
- **Managed Agents Memory API 안정화 헤더 `agent-memory-2026-07-22` 배포** — 메모리 조회 API 동작 표준화(안정 정렬, `depth` 제한, 경로 세그먼트 완전 일치). 2026-07-22부터 기존 `managed-agents-2026-04-01` 헤더도 동일 적용. Python·TS·Go·Java·Ruby 등 SDK 일괄 업데이트.

### 2026-07-01
- **Claude Fable 5·Claude Mythos 5 접근 복구** — 일시 중단 이후 Anthropic이 두 모델의 배포를 재개. [공식 성명](https://www.anthropic.com/news/redeploying-fable-5-mythos-5) 발표.

---

## Claude Code CLI 주요 릴리즈

| 버전 | 주요 사항 |
|------|---------|
| v2.1.220 | 버그 수정 및 안정성 개선 |
| v2.1.219 | **Claude Opus 5** 출시; Opus 4.7 Fast Mode 제거; 중첩 서브에이전트 depth 3 기본; `DirectoryAdded` 훅 |
| v2.1.218 | `/code-review` 배경 서브에이전트 전환; `/deep-research` 수동 호출 전환; Auto Mode 개선 |
| v2.1.217 | 이모지 단축 코드 자동완성; 동시 서브에이전트 캡 20개; 서브에이전트 중첩 기본 비활성화 |
| v2.1.216 | `sandbox.filesystem.disabled` 설정; 긴 세션 성능 개선; worktree git 리다이렉션 차단 수정 |
| v2.1.215 | `/verify`·`/code-review` 자동 실행 제거 — 명시 호출 필요 |
| v2.1.214 | 권한 체크 보안 강화; EndConversation 툴; 장시간 툴 하트비트; 메모리 `modified` 타임스탬프 |
| v2.1.212 | `/fork` 배경 세션 복사; `/subtask` 분리; WebSearch·서브에이전트 세션 캡; MCP 자동 배경 이동 |
| v2.1.211 | `--forward-subagent-text` 플래그; PreToolUse ask 우선순위 수정; 서브에이전트 모델 오버라이드 유지 |
| v2.1.210 | 툴 요약 경과시간 카운터; worktree 격리 개선; ultracode 비사람 입력 수정; 성능 개선 |
| v2.1.209 | `claude agents` 배경 세션 다이얼로그 차단 회귀 수정 |
| v2.1.208 | 스크린 리더 모드; `vimInsertModeRemaps`; `CLAUDE_CODE_PROCESS_WRAPPER`; 마우스 클릭 지원 |
| v2.1.207 | Auto Mode Bedrock·Vertex·Foundry GA; 터미널 프리즈 수정; 플러그인 훅 쉘 인젝션 차단 |
| v2.1.206 | `/cd` 경로 제안; `/doctor` CLAUDE.md 정리 제안; `/commit-push-pr` push 원격지 자동 허용 |
| v2.1.205 | Auto Mode 트랜스크립트 변조 차단; 다수 배경 에이전트 버그 수정 |
| v2.1.204 | SessionStart 훅 비대화형 세션 이벤트 스트리밍 수정 |
| v2.1.203 | 로그인 만료 경고; 수동 권한 모드 배지; MCP roots/list 업데이트; 다수 안정성 수정 |
| v2.1.202 | 동적 워크플로우 크기 설정; 워크플로우 OTel 속성 추가 |
| v2.1.201 | Claude Sonnet 5 세션 미드-대화 시스템 역할 하네스 리마인더 제거 |
| v2.1.200 | `AskUserQuestion` 자동 진행 기본 비활성화; 기본 권한 모드 → `"manual"`; 백그라운드 에이전트 다수 안정성 수정; 스크린 리더 접근성 개선 |
| v2.1.199 | `/skill-a /skill-b` 중첩 스킬 최대 5개 동시 로드; TLS 오류 즉시 실패 힌트; 부분 응답 미드스트림 보존; 백그라운드 에이전트 다수 수정 |
| v2.1.198 | 서브에이전트 백그라운드 실행 기본화(GA); **Claude in Chrome** 일반 공개; `/dataviz` 내장 스킬; Gateway에 Claude Platform on AWS 추가 |
| v2.1.197 | **Claude Sonnet 5** Claude Code 기본 모델 설정; 프로모션 $2/$10/MTok (2026-08-31까지) |
| v2.1.196 | 조직 기본 모델 지원; 세션 기본 이름 가독성 개선; 파일 첨부 Finder/Explorer 공개 지원 |
